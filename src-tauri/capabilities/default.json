{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "opener:default",
    "dialog:default",
    "dialog:allow-save",
    "fs:default",
    "fs:allow-app-write",
    "wdio:default"
  ]
}
